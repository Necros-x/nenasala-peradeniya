# Browser-safe Supabase values (RLS still required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SERVER ONLY — never prefix these with NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PORTAL_KEY=replace-with-a-long-random-server-only-value

# LOCAL DEV ONLY — ignored in production by the guard implementation
LOCAL_UI_BYPASS=false
